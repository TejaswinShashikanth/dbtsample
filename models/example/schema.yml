
version: 2

models:
    - name: dim_customer_marketing
      description: "Incre"
      columns:
        - name : _pk
          description : "Primary Key for the table"
          tests:
              - unique
              - not null
          
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
